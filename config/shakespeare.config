(streams
 (where (service #"^shakespeare")
        (rollup 1 2
                (elastic/es-index "shakespeare"
                                  :timestamping :month
                                  :index "shakespeare"))))
